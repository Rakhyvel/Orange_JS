module Main {
    struct ListNode(ListNode next, Any data);
    struct List(ListNode head, ListNode tail);

    void start() {
        ListNode node;
        ListNode node1 = ListNode(node, Any(char[255]));
    }
}