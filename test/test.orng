module test
    struct Vector(real x, real y)
    struct Entity(Vector pos)
    struct Player[Entity](real health)

    void main()
        Player p = Player(100.0)
        p.pos = Vector(0.0, 0.0)
    end
end