static module test
    ~~
    struct function:varMap
    i -> {type="int", ast=null, private=no, constant=no}
    j -> {type="int", ast=plus(4,5), private=no, constant=no}
    ~~
    void main()
        Vector pos = Vector:create(4.0, 4.0)
        real x = Vector:create(4.0, 4.0).x
    end
end

static module Vector
    struct Vector(real x, real y)

    Vector create(real x, real y)
    end
end