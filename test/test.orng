static module test
    int i = test2:g()

    ~~ A simple test program that asks for a username and password ~~
    void main()
        char array username = System:input("Username: ")
        char array password
        int tries = 0
        boolean in = false

        while tries < 5 and in isnt true
            if (password = System:input("Password: ")) is "password123"
                System:println("Correct! Welcome in")
                in = true
            else
                System:println("Wrong! Try again")
            end
        end
        
        if in
            displaySecrets()
        else
            System:println("Sorry! Goodbye")
            System:exit(1)
        end
    end

    void displaySecrets()
    end

    int f()
    end
end

module test2
    int g()
    end
end

static module System
    void println(char array msg)
    end

    char array input(char array prompt)
    end

    void exit(int errCode)
    end
end