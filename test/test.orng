static module Main {
    struct Person(char[] n, int a) {
        char[] name = n;
        int age = a;
    }

    struct Student:Person(char[] n, int a, real g) {
        super(n, a);
        real gpa = g;
    }

    void start() {
        Person[] p = Person[5];
        p[0] = Student("Joseph", 22, 4.0);
        graduate(cast(Student)p[0]);
    }

    void graduate(Student s) {
        
    }
}