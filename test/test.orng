static module Main {
    void start() {
        ~~ Main#Vector ~~
        Vector array v1 = Vector array[4];
        Vector pos;
        if 3 < 4 {
            
        } else {
            
        }
    }
    ~~ Main#Vector() ~~
    struct Vector(int x1, int y1) {
        int x = x1;
        int y = y1;
    }
}

module Apricot {
    struct Entity() {
        Vector pos = Vector(0, 0);
    }
}